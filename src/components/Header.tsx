import { FC, ReactElement } from 'react';
// import headerLogo from '../images/logo-light.svg';

const Header: FC = (): ReactElement => {
  return (
    <header className='header'>
      <img src='' alt='Логотип Местро' className='header__logo' />
    </header>
  );
};

export default Header;
